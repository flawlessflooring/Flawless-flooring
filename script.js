const menuBtn=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');menuBtn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const dialog=document.getElementById('lightbox'),dialogImg=document.getElementById('lightboxImg');document.querySelectorAll('.gallery-item').forEach(b=>b.addEventListener('click',()=>{dialogImg.src=b.dataset.full;dialog.showModal()}));document.getElementById('closeLightbox').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});
document.getElementById('quoteForm').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);const text=`Hi Flawless Flooring, I'd like a free quote.%0A%0AName: ${encodeURIComponent(d.get('name'))}%0APhone: ${encodeURIComponent(d.get('phone'))}%0APostcode: ${encodeURIComponent(d.get('postcode'))}%0AFlooring: ${encodeURIComponent(d.get('flooring'))}%0AJob details: ${encodeURIComponent(d.get('message')||'')}`;window.open(`https://wa.me/447932735579?text=${text}`,'_blank','noopener')});
